# Source the PDK xschemrc file.
source $env(PDK_ROOT)/$env(PDK)/libs.tech/xschem/xschemrc

# Make project libraries available regardless of the launch directory.
set XSCHEM_LIBRARY_PATH ${XSCHEM_LIBRARY_PATH}:$env(DESIGNS)

# Keep generated simulation data outside the source tree.
set netlist_dir "/foss/designs/simulations"
